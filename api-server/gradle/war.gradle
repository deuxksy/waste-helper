apply plugin: "war"

bootWar {
    mainClass = "com.waste.helper.WasteHelperApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]
}
